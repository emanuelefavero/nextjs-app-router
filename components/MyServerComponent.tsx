export default function MyServerComponent() {
  return (
    <>
      <h3>MyServerComponent</h3>
      <p>
        React components are server components by default. So even though this
        is not a page.tsx file, this still runs on the server
      </p>
    </>
  )
}
